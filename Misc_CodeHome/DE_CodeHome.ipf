#pragma rtGlobals=3		// Use modern global access method and strict wave access.
#pragma Modulename=DE_CodeHome

//This code exists SOLELY to identify where the code lives. The goal is, of course, to have everything
//in approximately one spot, but the Python code in particular tends to get lost